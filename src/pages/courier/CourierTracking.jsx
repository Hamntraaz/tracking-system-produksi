import { useEffect, useMemo, useRef, useState } from 'react'
import TrackingMap from '../../components/TrackingMap'
import Modal from '../../components/Modal'
import StatusBadge from '../../components/StatusBadge'
import { deliveryComplete, driverArrived, driverStart, updateDeliveryLocation } from '../../services/api'
import { isCloudinaryReady, uploadProofToCloudinary } from '../../services/cloudinary'

function SwipeToConfirm({ disabled, onUnlocked }) {
  const [dragging, setDragging] = useState(false)
  const [progress, setProgress] = useState(0)
  const [thumbX, setThumbX] = useState(0)
  const trackRef = useRef(null)
  function setFromClientX(clientX) { const rect = trackRef.current?.getBoundingClientRect(); if (!rect) return; const maxX = Math.max(0, rect.width - 58); const rawX = Math.max(0, Math.min(maxX, clientX - rect.left - 29)); setThumbX(rawX); setProgress(maxX ? rawX / maxX : 0) }
  function finishDrag() { setDragging(false); if (progress >= 0.86) { const rect = trackRef.current?.getBoundingClientRect(); setProgress(1); setThumbX(rect ? Math.max(0, rect.width - 58) : 0); onUnlocked?.(); window.setTimeout(() => { setProgress(0); setThumbX(0) }, 550) } else { setProgress(0); setThumbX(0) } }
  return <div ref={trackRef} className={`swipe-track ${disabled ? 'disabled' : ''} ${dragging ? 'dragging' : ''}`} onPointerMove={(event) => dragging && !disabled && setFromClientX(event.clientX)} onPointerUp={() => !disabled && finishDrag()} onPointerCancel={() => !disabled && finishDrag()}><span>{disabled ? 'Upload bukti foto dulu agar swipe aktif' : 'Geser ke kanan untuk konfirmasi selesai'}</span><button type="button" className="swipe-thumb" style={{ transform: `translateX(${thumbX}px)` }} onPointerDown={(event) => { if (disabled) return; event.currentTarget.setPointerCapture?.(event.pointerId); setDragging(true); setFromClientX(event.clientX) }} aria-label="Geser konfirmasi selesai">→</button></div>
}

function pointFromLocation(location) { if (!location) return null; const lat = Number(location.latitude); const lng = Number(location.longitude); if (!Number.isFinite(lat) || !Number.isFinite(lng)) return null; return { latitude: lat, longitude: lng, accuracy: Number(location.accuracy || 0), recorded_at: location.created_at } }
function makeTaskFromSupplier(row) { return { ...row, task_id: row.id, delivery_type: 'supplier_delivery', order_code: row.order_code, target_label: row.warehouse_name || 'Gudang', destination_role: 'warehouse' } }
function makeTaskFromBranch(row, data = {}) { const locations = data.actor_locations || {}; const branches = data.branches || []; const branch = branches.find((item) => String(item.id) === String(row.branch_id)) || branches[0]; const branchLoc = pointFromLocation(locations[`branch:${row.branch_id || branch?.id}`] || locations.branch); return { ...row, task_id: row.id, delivery_type: 'branch_request', order_code: row.code, target_label: row.branch_name || 'Cabang', destination_role: 'branch', destination_label: row.branch_name || branch?.name || 'Cabang', destination_lat: branchLoc?.latitude, destination_lng: branchLoc?.longitude, destination_address: branch?.address || row.branch_name || 'Lokasi cabang', recorded_at: row.updated_at, map_type: 'branch_request' } }

export default function CourierTracking({ data = {}, user, deviceLocation, locationStatus, locationError, requestLocation, startWatching, stopWatching, refreshData }) {
  const task = useMemo(() => {
    const courierId = user?.courier_id ? String(user.courier_id) : ''
    if (user?.courier_type !== 'warehouse') {
      const delivery = (data.deliveries || []).find((item) => (!courierId || String(item.courier_id || '') === courierId) && !['Pengiriman Selesai', 'Pesanan Diterima'].includes(item.status))
      if (delivery) return makeTaskFromSupplier(delivery)
    }
    if (user?.courier_type !== 'supplier') {
      const branch = (data.branch_requests || []).find((item) => item.courier_id && (!courierId || String(item.courier_id || '') === courierId) && !['Diterima Cabang', 'Ditolak Gudang', 'Ditolak Kurir'].includes(item.status))
      return branch ? makeTaskFromBranch(branch, data) : null
    }
    return null
  }, [data, user])
  const [trackingOn, setTrackingOn] = useState(false)
  const [sendStatus, setSendStatus] = useState('Menunggu instruksi perjalanan.')
  const [proofPhoto, setProofPhoto] = useState(task?.proof_photo || '')
  const [proofName, setProofName] = useState('')
  const [proofPreview, setProofPreview] = useState(task?.proof_photo || '')
  const [proofNote, setProofNote] = useState('')
  const [confirmChecked, setConfirmChecked] = useState(false)
  const [confirmVisible, setConfirmVisible] = useState(false)
  const [uploadingProof, setUploadingProof] = useState(false)
  const [uploadModal, setUploadModal] = useState({ open: false, stage: 'idle', progress: 0, message: '' })
  const [uploadResult, setUploadResult] = useState(null)
  const [busy, setBusy] = useState(false)
  const lastSentRef = useRef(0)
  const isInTransit = task?.status === 'Kurir Dalam Perjalanan'
  const isArrived = task?.status === 'Driver Sampai'
  const isWaitingStart = task && ['Menunggu Driver Berangkat', 'Tugas Diterima Kurir'].includes(task.status)
  const isDone = ['Pengiriman Selesai', 'Diterima Cabang'].includes(task?.status)
  const canUploadProof = isArrived && !isDone
  const apiRef = task?.delivery_type === 'branch_request' ? { requestId: task.task_id, deliveryType: 'branch_request' } : { deliveryId: task?.task_id, deliveryType: 'supplier_delivery' }

  async function sendPosition(position) { if (!task?.task_id) return; const now = Date.now(); if (now - lastSentRef.current < 3000) return; lastSentRef.current = now; const coords = position.coords || position; try { await updateDeliveryLocation({ ...apiRef, courierId: task?.courier_id, latitude: coords.latitude, longitude: coords.longitude, accuracy: coords.accuracy }); setSendStatus(`Lokasi tersimpan ${new Date().toLocaleTimeString('id-ID')}`); refreshData?.({ silent: true }) } catch (error) { setSendStatus(error.message || 'Gagal menyimpan lokasi ke database.') } }
  async function startDriver() { if (!task?.task_id) { setSendStatus('Belum ada tugas pengiriman aktif.'); return } setBusy(true); try { const position = deviceLocation ? { coords: deviceLocation } : await requestLocation(); const coords = position.coords || position; await driverStart({ ...apiRef, courierId: task?.courier_id, latitude: coords.latitude, longitude: coords.longitude, accuracy: coords.accuracy }); setSendStatus('Driver berangkat. Lokasi mulai dikirim otomatis.'); await refreshData?.(); startWatching((latestPosition) => sendPosition(latestPosition)); setTrackingOn(true) } catch (error) { setSendStatus(error?.message || 'Gagal memulai perjalanan. Cek izin lokasi browser/perangkat.') } finally { setBusy(false) } }
  async function markDriverArrived() { if (!task?.task_id) return; setBusy(true); setSendStatus('Menyimpan status driver sampai...'); try { const position = deviceLocation ? { coords: deviceLocation } : await requestLocation(); const coords = position.coords || position; await driverArrived({ ...apiRef, courierId: task?.courier_id, latitude: coords.latitude, longitude: coords.longitude, accuracy: coords.accuracy }); setSendStatus('Driver sampai. Silakan pilih dan upload foto bukti.'); await refreshData?.() } catch (error) { setSendStatus(error?.message || 'Gagal menyimpan status driver sampai.') } finally { setBusy(false) } }
  async function handleProofFile(event) { const file = event.target.files?.[0]; if (!file) return; if (file.size > 15 * 1024 * 1024) { setSendStatus('Ukuran bukti foto terlalu besar. Gunakan foto maksimal 15MB.'); return } const localPreview = URL.createObjectURL(file); setUploadingProof(true); setUploadResult(null); setProofName(file.name); setProofPreview(localPreview); setUploadModal({ open: true, stage: 'preparing', progress: 4, message: 'Menyiapkan foto bukti...' }); setSendStatus(isCloudinaryReady() ? 'Mengupload bukti foto ke Cloudinary...' : 'Menyimpan bukti foto ke database lokal...'); try { const uploaded = await uploadProofToCloudinary(file, { onProgress: (info) => setUploadModal((prev) => ({ open: true, stage: info.stage || prev.stage, progress: Number.isFinite(Number(info.progress)) ? Number(info.progress) : prev.progress, message: info.message || prev.message })) }); setProofPhoto(uploaded.url); setProofPreview(uploaded.url); setUploadResult(uploaded); setUploadModal({ open: true, stage: 'success', progress: 100, message: uploaded.storage === 'database' ? 'Bukti foto berhasil disimpan di database.' : 'Bukti foto berhasil diupload ke Cloudinary.' }); setSendStatus('Bukti foto berhasil. Swipe konfirmasi pengiriman sudah aktif.') } catch (error) { setProofPhoto(''); setUploadModal({ open: true, stage: 'error', progress: 0, message: error.message || 'Upload bukti foto gagal.' }); setSendStatus(error.message || 'Upload bukti foto gagal.') } finally { setUploadingProof(false); URL.revokeObjectURL(localPreview) } }
  async function completeDelivery() { if (!task?.task_id) return; if (!proofPhoto) { setSendStatus('Upload bukti foto dulu sebelum menyelesaikan pengiriman.'); return } if (!confirmChecked) { setSendStatus('Centang konfirmasi bahwa barang benar-benar sudah diterima.'); return } setBusy(true); try { const position = deviceLocation ? { coords: deviceLocation } : await requestLocation(); const coords = position.coords || deviceLocation; await deliveryComplete({ ...apiRef, courierId: task?.courier_id, latitude: coords.latitude, longitude: coords.longitude, proofPhoto, proofNote }); stopWatching?.(); setTrackingOn(false); setConfirmVisible(false); setConfirmChecked(false); setSendStatus('Pengiriman selesai. Bukti foto sudah tersimpan dan bisa dilihat role terkait.'); await refreshData?.() } catch (error) { setSendStatus(error.message || 'Gagal menyelesaikan pengiriman.') } finally { setBusy(false) } }
  useEffect(() => { if (!isInTransit || trackingOn) return undefined; let cancelled = false; (async () => { try { const position = await requestLocation?.(); if (cancelled) return; await sendPosition(position); startWatching?.((latestPosition) => sendPosition(latestPosition)); setTrackingOn(true); setSendStatus('Tracking live aktif. Lokasi dikirim otomatis ke database.') } catch (error) { setTrackingOn(false); setSendStatus(error?.message || 'Tracking belum aktif karena lokasi perangkat belum terbaca.') } })(); return () => { cancelled = true } }, [isInTransit, task?.task_id])
  useEffect(() => { setProofPhoto(task?.proof_photo || ''); setProofPreview(task?.proof_photo || '') }, [task?.proof_photo, task?.task_id])
  useEffect(() => () => stopWatching?.(), [stopWatching])

  return <><section className="page-head-card"><div><span>Kurir</span><h2>Live Tracking Pengiriman</h2><p>Maps hanya menampilkan titik kurir aktif dan titik tujuan. Foto bukti wajib diupload sebelum pengiriman bisa diselesaikan.</p></div>{task && <StatusBadge>{task.status}</StatusBadge>}</section><section className="content-grid two-one"><article className="panel-card wide"><div className="panel-head compact"><div><span>Maps Live</span><h3>{task?.order_code || 'Belum ada pengiriman aktif'}</h3></div><StatusBadge>{trackingOn ? 'Tracking Aktif' : 'Tracking Tidak Aktif'}</StatusBadge></div><TrackingMap delivery={task} deviceLocation={deviceLocation} showUserLocation viewerUser={user} hidePickup useDeviceAsCurrent currentLabel="Posisi Saya" />{!trackingOn && isInTransit && <p className="warning-box">Maps belum menerima lokasi kurir. Pastikan GPS perangkat aktif dan halaman ini tetap terbuka.</p>}</article><article className="panel-card"><div className="panel-head"><div><span>Kontrol Kurir</span><h3>Perjalanan & Bukti Terima</h3></div></div><div className="tracking-control-stack"><button type="button" className="login-button" onClick={startDriver} disabled={busy || !task || !isWaitingStart}>{busy ? 'Memproses...' : 'Driver Berangkat'}</button><button type="button" className="secondary-action full" onClick={markDriverArrived} disabled={busy || !task || !isInTransit}>{busy ? 'Memproses...' : 'Driver Sampai'}</button></div><div className="detail-stack location-detail compact-detail"><p><b>Status izin lokasi</b><span>{locationStatus}</span></p><p><b>Tujuan</b><span>{task?.destination_address || task?.target_label || '-'}</span></p><p><b>Latitude</b><span>{Number.isFinite(Number(deviceLocation?.latitude)) ? Number(deviceLocation.latitude).toFixed(6) : '-'}</span></p><p><b>Longitude</b><span>{Number.isFinite(Number(deviceLocation?.longitude)) ? Number(deviceLocation.longitude).toFixed(6) : '-'}</span></p><p><b>Status sistem</b><span>{sendStatus}</span></p></div><div className="proof-box"><label>Bukti Foto Saat Sampai</label><input type="file" accept="image/*" capture="environment" onChange={handleProofFile} disabled={!canUploadProof || uploadingProof} />{proofName && <small>File: {proofName}</small>}{!canUploadProof && !isDone && <p className="helper-box">Tekan Driver Sampai terlebih dahulu agar upload bukti foto aktif.</p>}{uploadingProof && <p className="helper-box upload-inline"><span className="mini-spinner" /> Foto sedang diupload. Jangan tutup halaman ini.</p>}{proofPreview && <img src={proofPreview} alt="Preview bukti pengiriman" />}{proofPhoto && <a className="proof-link" href={proofPhoto} target="_blank" rel="noreferrer">Lihat file bukti</a>}<textarea value={proofNote} onChange={(e) => setProofNote(e.target.value)} placeholder="Catatan opsional" disabled={!canUploadProof} /></div><div className="completion-card"><SwipeToConfirm disabled={!canUploadProof || !proofPhoto || busy || uploadingProof} onUnlocked={() => setConfirmVisible(true)} />{confirmVisible && <div className="confirm-arrival-box"><label><input type="checkbox" checked={confirmChecked} onChange={(e) => setConfirmChecked(e.target.checked)} disabled={busy} /> Saya memastikan barang sudah diserahkan di tujuan dan bukti foto sudah benar.</label><button type="button" className="login-button" onClick={completeDelivery} disabled={!confirmChecked || busy}>{busy ? 'Menyimpan...' : 'Konfirmasi Pengiriman Selesai'}</button></div>}</div>{locationError && <p className="error-box">{locationError}</p>}{!isCloudinaryReady() && <p className="warning-box">Cloudinary belum dikonfigurasi. Sistem memakai mode gratis: foto dikompres lalu disimpan di database.</p>}</article></section><Modal open={uploadModal.open} title={uploadModal.stage === 'success' ? 'Upload Bukti Berhasil' : uploadModal.stage === 'error' ? 'Upload Bukti Gagal' : 'Mengupload Bukti Foto'} onClose={() => { if (uploadingProof) return; setUploadModal((prev) => ({ ...prev, open: false })) }}><div className={`upload-modal-content ${uploadModal.stage}`}><div className="upload-visual" aria-hidden="true">{uploadModal.stage === 'success' ? '✓' : uploadModal.stage === 'error' ? '!' : <span className="upload-spinner" />}</div><h3>{uploadModal.message || 'Memproses bukti foto...'}</h3>{uploadModal.stage !== 'error' && <div className="upload-progress-wrap"><div className="upload-progress-bar" style={{ width: `${Math.max(0, Math.min(100, uploadModal.progress || 0))}%` }} /></div>}<p className="upload-percent">{uploadModal.stage === 'error' ? 'Coba pilih foto ulang atau gunakan jaringan yang lebih stabil.' : `${Math.round(uploadModal.progress || 0)}%`}</p>{proofPreview && <img className="upload-preview" src={proofPreview} alt="Preview bukti pengiriman" />}{uploadModal.stage === 'success' && uploadResult && <div className="upload-result-detail"><span>{uploadResult.storage === 'database' ? 'Database lokal' : 'Cloudinary tersimpan'}</span><span>{uploadResult.width} × {uploadResult.height}px</span><span>{uploadResult.uploadedSize ? `${Math.max(1, Math.round(uploadResult.uploadedSize / 1024))} KB` : 'Ukuran aman'}</span></div>}{!['uploading', 'preparing', 'compressing'].includes(uploadModal.stage) && <button type="button" className="login-button" onClick={() => setUploadModal((prev) => ({ ...prev, open: false }))}>Tutup</button>}</div></Modal></>
}
